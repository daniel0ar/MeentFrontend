import React from 'react';
import styles from "../styles/showcase.module.scss";

const Showcase = () => {
    return (
        <div className={styles.showcase}>
            <h1>Welcome To The Party!</h1>
            <h2>Find all your local events here</h2>
        </div>
    );
}

export default Showcase;
